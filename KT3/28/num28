import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();

        int i = 1;
        int squareDiff = 0;

        while (squareDiff < n) {
            squareDiff = (i + 1) * (i + 1) - i * i;
            i++;
        }

        System.out.println(squareDiff);
    }
}
